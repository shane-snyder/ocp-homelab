apiVersion: oadp.openshift.io/v1alpha1
kind: DataProtectionApplication
metadata:
  name: oadp-backup
  namespace: openshift-adp
spec:
  configuration:
    velero:
      defaultPlugins:
        - openshift
        - aws
      featureFlags:
        - EnableCSI
    restic:
      enable: true
  backupLocations:
    - name: oadp-backup
      velero:
        provider: aws
        default: true
        objectStorage:
          bucket: oadp-backup
          prefix: backups
        config:
          region: minio
          s3Url: http://minio-openshift-adp.apps.sno.shanehomelab.com
          insecureSkipTLSVerify: "true"
          s3ForcePathStyle: "true"
        credential:
          name: cloud-credentials
          key: cloud