apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: rhel10-dnf-check
  namespace: rhel10-vm
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  labels:
    app: rhel10-dnf-check
spec:
  running: true
  template:
    metadata:
      labels:
        kubevirt.io/domain: rhel10-dnf-check
        # role label drives MultiNetworkPolicy podSelector matching once policies
        # are authored from discovery (Phase 3).
        role: app
    spec:
      domain:
        cpu:
          cores: 1
          sockets: 1
          threads: 1
        devices:
          disks:
            - disk:
                bus: virtio
              name: rootdisk
            - disk:
                bus: virtio
              name: cloudinitdisk
          interfaces:
            - masquerade: {}
              name: default
            # Secondary interface on the OVN-K localnet network. OVN-K assigns
            # the IP from the NAD's subnet (IPAM), so no static config needed.
            - bridge: {}
              name: vlan100
          rng: {}
        resources:
          requests:
            memory: 2Gi
      networks:
        - name: default
          pod: {}
        # Cross-namespace reference to the NAD in vm-network. NOTE: the VM pod
        # runs in the rhel10-vm namespace, so MultiNetworkPolicy resources that
        # select this VM must also live in rhel10-vm (annotated policy-for
        # vm-network/vlan100), not in vm-network.
        - name: vlan100
          multus:
            networkName: vm-network/vlan100
      volumes:
        - dataVolume:
            name: rhel10-dnf-check-rootdisk
          name: rootdisk
        - cloudInitNoCloud:
            secretRef:
              name: rhel10-dnf-check-cloudinit
          name: cloudinitdisk
  dataVolumeTemplates:
    - metadata:
        name: rhel10-dnf-check-rootdisk
      spec:
        sourceRef:
          kind: DataSource
          name: rhel10
          namespace: openshift-virtualization-os-images
        storage:
          resources:
            requests:
              storage: 30Gi
          storageClassName: lvms-dell
