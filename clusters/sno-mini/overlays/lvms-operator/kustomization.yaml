apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-storage

resources:
  - ../../../../components/lvms-operator

patchesStrategicMerge:
  - device-classes-patch.yaml

patchesJson6902:
  - target:
      group: operators.coreos.com
      version: v1alpha1
      kind: Subscription
      name: lvms-operator
      namespace: openshift-storage
    path: channel-patch.yaml
