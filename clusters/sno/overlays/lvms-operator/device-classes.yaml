apiVersion: lvm.topolvm.io/v1alpha1
kind: LVMCluster
metadata:
  name: lvmcluster
  namespace: openshift-storage
spec:
  storage:
    deviceClasses:
      - default: false
        deviceSelector:
          paths:
            - /dev/sdc
        fstype: xfs
        name: minio
        thinPoolConfig:
          name: thin-pool-2
          overprovisionRatio: 10
          sizePercent: 90
      - default: true
        deviceSelector:
          paths:
            - /dev/sdb
        fstype: xfs
        name: dell
        thinPoolConfig:
          name: thin-pool-1
          overprovisionRatio: 10
          sizePercent: 90
